#! /usr/bin/env bash
set -e -x
SCRIPT_DIR=$(cd $(dirname $0); pwd)
cd ${SCRIPT_DIR}

if [ -p /dev/stdin ]
then
    exec docker exec -i mysqld /bin/mysql < /dev/stdin
else
    exec docker exec -it mysqld /bin/mysql
fi
